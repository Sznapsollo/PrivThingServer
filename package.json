{
  "name": "PrivThingServer",
  "version": "1.0.0",
  "description": "Simple Web server to serve PrivThing and also allow it to work on files from specified folders on current system",
  "main": "app.js",
  "scripts": {
    "start": "node app",
    "server": "nodemon",
    "client": "npm start --prefix client",
    "build": "npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-fetch": "^3.1.4",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-validator": "^6.10.0"
  },
  "devDependencies": {
    "concurrently": "^6.0.2"
  }
}
